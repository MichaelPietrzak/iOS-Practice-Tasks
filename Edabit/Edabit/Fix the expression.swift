//
//  Fix the expression.swift
//  Edabit
//
//  Created by Michal Pietrzak on 26/01/2024.
//

import Foundation

// Fix the Expression
// Fix the code in the Code tab so the function returns true if and only if x is equal to 7. Try to debug code and pass all the tests.

struct CheckExpression {
    func isSeven(_ num: Int) -> Bool {
        num == 7 ? true : false
    }
}
